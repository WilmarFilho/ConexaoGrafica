# Hub Editora

As orientações para agentes e sessões de IA estão em `CLAUDE.md` (mesma pasta).
Resumo: tudo roda em Docker (`docker compose exec app php artisan ...`), alvo
de produção PHP 8.3 em cPanel, sem Node/Redis no servidor. Não instale PHP
localmente.
